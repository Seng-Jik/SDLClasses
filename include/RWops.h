#pragma once
#include <string>
/*
namespace SDL
{
	using std::string;

	class RWops final
	{
	public:
		RWops(void* mem, int size);
		RWops(const void* mem, int size);
		RWops(const string& filePath);
		~RWops();

		RWops(const RWops&) = delete;
		RWops(RWops&&) = default;
		RWops& operator = (RWops&&) = default;
		RWops& operator = (const RWops&) = delete;
		
	private:
		void* rwopsHandle_;
	};
}*/